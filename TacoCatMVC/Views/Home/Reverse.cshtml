@model Palindrome

<div class="container col-xxl-8 px-4 py-5 mt-5">
    <div class="row gy-2 h-100 row-cols-md-2 row-cols-1">
        <div class="col border-2 border-end">
            <h1 class="fw-bold lh-1 mb-3">Palindrome Check/(String Reversal)</h1>
            <h4 class="lead">
                Here we reverse a string and test it as a palidnrome.
            </h4>
            <h5>
                * Palindrome- a word, phrase or sequence that  reads the same backwards as it does forwards.
            </h5>
            <h2 class="mt-5 border-bottom border-2">Instructions</h2>
            <p>Enter a word or sentence. Press the button and the input will be evaluated. If the word is a Palidrome then the reversed result will appear in <span style="color:green;font-weight:bold">GREEN.</span>Else, if the reveresed word is not a Palindrome the reveresed result will appear in <span style="        color: red;
        font-weight: bold">RED.</span></p>
        </div>
        <div class="col">
            <div>
                <h5>Enter a string</h5>

                <form class="row gy-2 gx-3" asp-action="Index" asp-controller="Home" method="Post">
                    <div class="col">
                        <input asp-for="InputWord" class="form-control" value="@Model.InputWord" placeholder="* Type a word" />
                    </div>
                    <div class="col-12 d-flex align-items-center justify-content-start justify-content-lg-end">
                        <button id="btnSubmit" type="submit" class="btn btn-dark btn-lg">
                            Palindrome Test
                        </button>
                    </div>
                </form>


                <h5 class="mt-5 border-bottom border-2">Reversed Result</h5>
                <div class="col">
                    @if (Model.IsPalindrome == true)
                    {

                        <p class="form-control" style="color: green">@Model.RevWord</p>
                    }
                    else
                    {

                        <p class="form-control" style="color: red">@Model.RevWord</p>
                    }
                </div>
                <h2 class="mt-5 border-bottom border-2">Palindrome Result</h2>
                <div class="table-responsive">
                    <p id="results">
                        @Model.Message
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>